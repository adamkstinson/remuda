# Remuda project agents

Each Planet deliverable against this product gets an agent directory here:

```
~/Projects/remuda/                    # product git root; the mounted workspace
  projects/
    remu/                             # REMU (this opening deliverable)
```

The brain is `projects/<ident>/`. The gem lives at the repo root. Secrets and
agent state are gitignored.
